rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read access to everyone for blog images
      allow read: if true;
      
      // Only authenticated admins can write (upload/delete)
      allow write: if request.auth != null;
    }
  }
}
